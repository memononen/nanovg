IF(BIICODE)
	find_path(GLES2_INCLUDE_DIR GLES2/gl2.h)
	if(NOT GLES2_INCLUDE_DIR AND ${BII_example_example_gles2_TARGET} NOT STREQUAL "")
		LIST(REMOVE_ITEM BII_BLOCK_EXES example_example_gles2)
	endif()

	find_path(GLES3_INCLUDE_DIR GLES3/gl3.h)
	if(NOT GLES3_INCLUDE_DIR AND ${BII_example_example_gles2_TARGET} NOT STREQUAL "")
		LIST(REMOVE_ITEM BII_BLOCK_EXES example_example_gles3)
	endif()

	INCLUDE(biicode/cmake/tools)
	ADD_BIICODE_TARGETS()
	TARGET_COMPILE_OPTIONS(${BII_BLOCK_TARGET} INTERFACE -D_CRT_SECURE_NO_WARNINGS)

	IF(APPLE)
		ADD_OSX_FRAMEWORK(OpenGL ${BII_LIB_TARGET})
		ADD_OSX_FRAMEWORK(Cocoa ${BII_LIB_TARGET})
		ADD_OSX_FRAMEWORK(IoKit ${BII_LIB_TARGET})
		ADD_OSX_FRAMEWORK(CoreVideo ${BII_LIB_TARGET})
	ENDIF()

	IF(WIN32)
		# Apply to lib
		TARGET_COMPILE_OPTIONS(${BII_BLOCK_TARGET} INTERFACE -DNANOVG_GLEW)
	ENDIF()


ENDIF()
